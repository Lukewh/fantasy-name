{
  "name": "fantasy-names",
  "version": "0.0.1",
  "description": "Insert a random fantasy name.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production && mkdir build && cp main.js build/main.js && cp manifest.json build/manifest.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.3.2",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.18.10",
    "obsidian": "^1.2.8",
    "tslib": "^2.6.0",
    "typescript": "^5.1.6"
  }
}
